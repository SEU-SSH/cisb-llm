现在我有一份小的数据集，里面包含从 Linux Kernel Commit 中提取的对应 message 和 code patch 相关内容：
- P_commits.json：66 条表示真实存在 CISB 的 commit 数据。
- N_commits.json：54 条表示不存在 CISB 的 commit 数据。

目前我已经搭建好了最小化的 RAG 框架，想要测试使用 RAG 简化原来的 Reasoner 推理过程。

具体而言是让 LLM 在判断是否存在 CISB 之前和推理过程中，利用 RAG 从向量数据库中查询对应的定义、规范、相关概念区分和确定 CISB 的判据。减少提示词中对应的硬编码，减轻在推理过程中可能出现的幻觉，前后矛盾和思维链断裂。

现在我想要做一个对比实验，主要是测试加上 RAG 之前和之后的判断效果。我的想法是：
- 不用 RAG：使用原来的的提示词，不做修改。
- 使用 RAG：基于原来的提示词修改得到新的提示词。基于最小化 RAG 根据的知识库，删去原始的提示词中可以用 RAG 替换的部分，转为在 Reasoner 执行前从 RAG 中检索相关信息。

验证最小化的 RAG 的效果，主要看在阳性和阴性样本上的对应准确率。

主要流程是：
1. 先执行测试阳性样本，先推理不用 RAG、休眠半个小时之后推理使用最小 RAG。
2. 后执行测试阴性样本，顺序同上。
3. 将生成的分析报告（.md 文件）按照使用或不使用 RAG、阳性和阴性样本的测试放在 @results 的多级目录下，例如：
    - NoRAG
        - P
        - N
    - MinimalRAG
        - P
        - N

请你为我设计一个实现方案，最后用一个脚本实现这个对比执行。
由于这个推理过程可能会很长，你不需要实际让这个 LLM 的推理过程跑起来，最后的实验执行交给我。